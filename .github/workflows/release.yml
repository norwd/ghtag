---

name: "Create Release"
run-name: "Create Release `${{ inputs.tag }}`"
permissions: {}

on:
  workflow_dispatch:
    inputs:
      tag:
        required: true
        description: "The tag to create."

defaults:
  run:
    shell: bash

jobs:
  tag:
    name: "Tag ${{ inputs.tag }}"
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: "Tag ${{ inputs.tag }} at ${{ github.ref }}"
        uses: norwd/tagme@345ff682ca482e009b088e3ed65184594c4ba4f3
        with:
          tag: ${{ inputs.tag }}
